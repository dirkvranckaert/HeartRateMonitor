<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Material.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:colorAccent">@color/hrm_accent_color</item>
        <item name="android:colorPrimary">@color/hrm_primary_color</item>
        <item name="android:colorPrimaryDark">@color/hrm_primary_dark_color</item>
    </style>

</resources>
